export async function storeMemory(userId: string, characterId: string, content: string, category: string, importance: number): Promise<void> {
  return;
}

export async function retrieveRelevantMemories(userId: string, characterId: string, queryText: string, limit: number = 3): Promise<string[]> {
  return [];
}
